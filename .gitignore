# 环境变量（含 API Key，绝不上传）
.env
backend/.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# 向量库（运行时产物，可重建）
backend/chroma_db/
backend/data/chroma/
backend/data/vectorstore/

# 连接器实时同步产生的数据（每次 sync 覆盖生成，不入库）
backend/data/docs/**/live_*.md

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 日志
*.log
logs/

# 内部工作记忆（本地 Agent 状态，不入库）
.workbuddy/

# 批量任务运行时产物（临时落盘，TTL 24h 惰性清理，不入库）
backend/data/jobs/

# 流水线运行期状态与生成的日报（可重跑重建）
backend/data/pipeline_state.json
backend/data/reports/

# Vite 临时文件
frontend/vite.config.ts.timestamp-*

# 求职/简历/面试类文档（含真实姓名与联系方式，仅本地保留，不入库）
简历*
面试*
JD解析*
君睿诚*
*投递版*
飞书*
