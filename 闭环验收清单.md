# 闭环验收清单（一页纸，打勾用）

## 什么算"闭环完成"

**判定标准只有一条：飞书表格里「补货建议 / 建议下单量 / 建议时间」三列，出现了 AI 生成的值。**

因为完整链路是：
```
表格有数据 → 脚本筛出预警行 → 调 AI 工作台 → 建议回写表格 → 人能看到
```
最后一步"人能看到"没发生，链路就还是断的。

### 三个等级（做到哪算哪，都能讲）

| 等级 | 完成到哪 | 能说什么 |
|---|---|---|
| 🥉 铜 | Step 0–2 | "我搭了表，并用 AI 工作台跑出了补货建议"（有终端截图） |
| 🥈 银 | + Step 3–7 | "我接通了飞书 API，能读到真实表格、筛出预警行并调通 AI"（有 dry-run 输出） |
| 🥇 金 | + Step 8 | **闭环完成**：AI 建议真的写回了表格（表格截图 + 能讲清"触发→动作→回写"） |

---

## Step 0 · 确认表没问题

- [ ] 「可用天数」列有数字（不是 `#ERROR`）
- [ ] 「预警状态」列出现 4 种值：紧急补货 / 建议补货 / 继续观察 / 库存充足
- [ ] 10 行数据都在

> 显示 `#ERROR` → 中括号写成中文 `【】` 或逗号是中文全角，双击单元格改公式。

## Step 1 · 启动工作台

- [ ] 双击 `启动.bat`，看到 `Uvicorn running on http://127.0.0.1:8000`
- [ ] **这个黑窗口一直开着，别关**

> 报 WinError 10048（端口占用）→ `netstat -ano | findstr :8000` 找 PID → `taskkill /PID <PID> /F` → 重开。

## Step 2 · 先跑 demo（不碰飞书，先确认 AI 那一半是通的）

```bat
cd /d D:\code\ecommerce-ai-workbench\examples\feishu-bitable
set WORKFLOW_API_KEY=dev-local-key-2026
D:\code\ecommerce-ai-workbench\backend\.venv\Scripts\python.exe bitable_sync.py --demo
```

- [ ] 看到 `成功 2 行｜失败 0 行` 和两行建议（KB-8800 / MIC-Lavalier-Pro）
- [ ] **截图保存**（这张是铜级证据）

> `连不上工作台` → 服务窗口关了，回 Step 1。`HTTP 503` → 改过 `.env` 没重启，关掉重开。

## Step 3 · 建飞书应用（拿两把钥匙）

- [ ] 打开 `open.feishu.cn` → 右上角「开发者后台」→ 创建**企业自建应用**（名字如 `AI工作台同步`）
- [ ] 左侧「凭证与基础信息」→ 复制 **App ID**（cli_xxx）和 **App Secret**
- [ ] 自检：下面这条命令返回里有 `tenant_access_token`（把 cli_xxx 换成你的）

```bat
curl -X POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal -H "Content-Type: application/json" -d "{\"app_id\":\"cli_xxx\",\"app_secret\":\"你的secret\"}"
```

> 报 `invalid app_secret` → 复制漏了字符。报权限不足 → Step 4 没做完。

## Step 4 · 开权限 + 发布版本（最容易漏）

- [ ] 左侧「权限管理」→ 搜索勾选 `bitable:app`、`bitable:app:readonly`
- [ ] 顶部「创建版本」→ 版本号填 `1.0.0` → 申请发布 → 到「版本管理与发布」里**通过审批**

> **不发布 = 权限不生效 = 后面全部 403。** 个人团队你自己就是管理员，自己点通过。

## Step 5 · 把应用加为表格协作者（最常见的坑）

- [ ] 回到多维表格 → 右上角「分享」或「协作者」
- [ ] 搜索你的应用名 `AI工作台同步` → 添加 → 权限选「**可编辑**」

> 漏这步：能读到数据，但回写时报权限错误。

## Step 6 · 取两个 ID

打开表格，URL 形如：
`https://xxx.feishu.cn/base/AppTokenXxx?table=tblXxx&view=vewXxx`

- [ ] `FEISHU_BITABLE_APP_TOKEN` = `/base/` 后面那串（AppTokenXxx）
- [ ] `FEISHU_BITABLE_TABLE_ID` = `?table=` 后面那串（tblXxx）

## Step 7 · dry-run（先看不改，验证能读到真实表格）

```bat
cd /d D:\code\ecommerce-ai-workbench\examples\feishu-bitable
set PY=D:\code\ecommerce-ai-workbench\backend\.venv\Scripts\python.exe
set WORKFLOW_API_KEY=dev-local-key-2026
set FEISHU_APP_ID=cli_你的
set FEISHU_APP_SECRET=你的
set FEISHU_BITABLE_APP_TOKEN=你的
set FEISHU_BITABLE_TABLE_ID=你的
%PY% bitable_sync.py
```

- [ ] 第一行显示 `已拉取 10 条记录`
- [ ] **命中 6 行**（3 紧急 + 3 建议），跳过 4 行
- [ ] 每行都给出了紧急度和建议量

> 命中数不对 → 字段名和表里的不一致（对照 Step 0 的字段名）。
> 报权限/403 → 回 Step 4、5。

## Step 8 · 回写（就这一步，闭环完成）

```bat
%PY% bitable_sync.py --apply
```

- [ ] 提示 `已回写 6 行到飞书多维表格`
- [ ] **切到飞书表格看**：「补货建议」有句子、「建议下单量」有数字、「建议时间」有时间
- [ ] **截图保存**（这张最值钱）

**到这一步，闭环完成。🥇**

## Step 9 · 能讲出来（不然等于没做）

- [ ] 出声念 3 遍 30 秒讲稿（在 `飞书多维表格_3小时补齐方案.md` 第 5.1 节）
- [ ] 能不卡壳地说出：**触发条件**（可用天数 < 30）→ **动作**（调 Webhook 拿建议）→ **回写**（写回三列）
- [ ] 能接住这句追问："为什么不用公式直接算完？" → "数字归公式，判断归模型"

---

## 卡住就停的规则

| 卡在哪 | 怎么办 |
|---|---|
| Step 3–6 超过 30 分钟 | 停在铜级，带 demo 截图去面试，一样能讲 |
| Step 7 报权限错误 | 检查 Step 4（发布了吗）、Step 5（加协作者了吗） |
| Step 8 报错 | dry-run 的结果已经能当银级证据，截图保存即可 |

**别为了最后一步耗掉整个晚上。** 铜级 + 讲得清楚，比金级 + 讲不清楚得分高。
