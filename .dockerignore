# ============================================================
# 不进入 Docker 构建上下文：既缩小构建体积，也防止把密钥打进镜像层
# 关键：backend/.env 里有真实 LLM_API_KEY，必须排除，否则 COPY backend/ ./ 会把它固化进镜像
# ============================================================

# 密钥与本地环境
**/.env
**/.env.local
**/.env.*
!.env.example

# 版本控制与编辑器
.git
**/.git
**/.idea
**/.vscode
**/.workbuddy

# 依赖与缓存（前端/后端都在镜像内重新安装，不需要上下文里的）
**/node_modules
**/__pycache__
**/*.pyc
**/.pytest_cache
**/.mypy_cache

# 运行时数据：持久卷与本地向量索引不应进镜像
**/chroma_db
**/*.sqlite3
**/*.bin

# 测试与文档：运行时镜像不需要
backend/tests
**/*.md
