# 面试介绍指南 · 跨境电商 AI 运营工作台（核心项目）

> 适用岗位：后端开发 / AI 应用开发（深圳，应届 9-12K）
> 基于简历事实 + 真实可跑代码定制，不要照背，要内化为自己的话

---

## 一、自我介绍（60-90 秒，定调）

> 模板（按需增减）：
>
> 面试官您好，我叫李文宇，广州软件学院 2026 届软件工程专业。
> 我对**后端 + AI 应用**方向比较感兴趣，今年暑期在深圳市优联软件做过 4 个月 Java + LangChain4j 的 AI 应用实习；校招前又独立交付了一个完整的跨境电商 AI 运营工作台作为个人核心项目——**前端、后端、知识库、6 个业务 Agent、文档检索评测、容器化上线**都是一个人完成。
> 我比较熟悉 AI 工程化里几个具体问题：RAG 检索质量、模型路由、按复杂度选模型降本、以及模型输出不稳定时的容错解析。
> 今天主要想聊我这个项目的设计与几个关键决策。

要点：
- 30 秒内让面试官听到三个关键词：**后端 / AI 应用 / 一人全栈**
- 不要罗列课程和证书
- 不要说"性格开朗、积极向上"这类废话

---

## 二、核心项目讲解（5-7 分钟，按 STAR 变体）

### 1. 项目是什么、为什么做（30 秒）

> 我做的是面向跨境电商运营的 AI 工作台，把选品、Listing 撰写、评论洞察、广告诊断、物流方案、客服话术这 6 个高频决策环节，整合到一套 RAG + 多 Agent 系统里。
> 业务痛点是：运营每件事都要查方法论 + 凭经验判断，效率低且依赖个人。我用"领域知识库检索 + 大模型"把方法论沉淀成可独立调用的智能体。
> 关键设计目标：**演示要稳、要省钱、能换模型、能接真实店铺数据**。

### 2. 架构怎么分（1 分钟，建议手画或口述层次）

```
┌────────────────────────────────────────────────┐
│ 前端  React 18 + Vite + TS（动态渲染智能体面板）  │
├────────────────────────────────────────────────┤
│ 控制器  app/api    只做请求解析、调用服务、返回     │
│ 服务层  app/services  Agent 编排、知识库管理      │
│ 智能体  app/agents    6 个 Agent 共享一条链路     │
│ 基础设施 app/core     LLM 网关/向量库/切分/重排   │
└────────────────────────────────────────────────┘
```

要主动讲出几个分层原则（让面试官知道你懂架构）：
- 控制器**不写业务判断**，业务判断一律在服务层
- 服务层**不依赖 HTTP 类型**，可以被脚本直接复用
- 智能体公共链路**收敛在 BaseAgent**，新增一个 Agent 只写 `build_prompt + output_schema` 两个方法
- 配置**集中在 config.py**，启动时校验，缺失密钥快速失败（不要静默降级）

### 3. 三个最值得讲的难点（各 1-1.5 分钟）

#### 难点 1：RAG 检索质量（高频追问点）

> 我做的不是简单的"按字符切 + 向量召回"，而是：
> - **段落优先**：先按空行分段，完整段落独立成块——因为拦腰切段落会破坏语义；
> - **长段按句切**：超过阈值时按句末标点切句，滚动累积到接近阈值；
> - **重叠只加在"长段被切断"的位置**：完整段落之间不补，避免切片被上一段内容污染、向量被稀释。
> - **向量召回 + 词面重排**：召回 2 倍候选后，用 `α·语义分 + (1-α)·词面分` 融合重排，缓解"字面命中却被排后"的问题。融合权重 α=0.7，纯本地、不联网、确定可复现。

诚实补一句：我在代码评审时发现了原版切分对每个块都无差别补重叠，会污染中文短段落，已经修了。

#### 难点 2：模型路由（高频追问点）

> 6 个 Agent 复杂度差异很大（判断类 vs 分析类），全部用最强模型成本不现实。
> 我做了按关键词 + 文本长度的规则路由：
> - 命中"分析/诊断/生成/策略"等关键词加权重；
> - 命中"分类/判断/提取"等减权重；
> - 文本超过 200 字直接判为复杂；
> - 综合得分 > 0 走重量模型 qwen-max，否则走 qwen-plus。
>
> 前端成本看板能看到**这次调用实际走了哪个模型、用了多少 token**，让"省钱"变成可验证的事。

被追问"为什么不用小模型做路由"的标准答案：
> 规则路由零成本、可复现、面试可以讲清楚。小模型路由要再花一次调用、引入额外故障域，且分类准确率依赖训练数据。规则够用的场景不必上模型——这是我整个项目的取舍原则：**先简单方案，验证不够用再升级**。

#### 难点 3：三级 JSON 容错（高频追问点）

> 模型输出不是 JSON 是常态。三级容错：
> - 第一级：直接 JSON.parse；
> - 第二级：去掉 Markdown 围栏（模型经常包 ```json```）再解析；
> - 第三级：按括号配对扫描出第一个完整的 {...}（不是正则，因为字符串里也有花括号）。
>
> 三级都失败**直接报 502**，绝不把脏数据透传给前端——前端拿到半截 JSON 会直接崩。
> 关键原则：**宁可报错，也不让脏数据到前端**。

### 4. 工程化落地（30-45 秒）

- **单容器交付**：Dockerfile 多阶段构建，Node 只用于构建前端，运行时只有 Python；前端构建产物由 FastAPI 统一托管
- **多厂商抽象**：百炼 / DeepSeek / OpenAI / Ollama 都走 OpenAI 兼容协议，切换只改 .env 业务代码零改动
- **结构化 JSON 日志 + 请求 ID 贯穿**：一次请求所有日志可串联
- **类型化错误体系 + 全局处理器**：客户端只看到规范化的 `{error: {code, message, request_id}}`，绝不返回堆栈
- **健康检查 + 就绪探针**：配置缺失时 `/api/health` 返回 503 + 中文原因，`/api/ready` 永远 200 + ready=false
- **容器加固（生产就绪）**：运行时以非 root 的 `appuser` 启动（入口脚本先 chown 持久卷再用 `runuser` 降权）；`HEALTHCHECK` 复用 `/api/ready` 探针；`.dockerignore` 防止密钥进入镜像层
- **测试**：77 个 pytest 用例，覆盖核心逻辑（解析容错、切分、重排、路由、配置校验、服务层错误类型、**SSE 事件契约**）；流式接口有专门的集成测试验证事件顺序与异常收敛

### 5. 项目现状

- 21 篇文档 → 299 个切片，检索评测 6/6 = 100% 覆盖
- 内置 Shopify 连接器，可把真实订单/评论拉进知识库
- 单机可演示，生产部署前需补 JWT/限流/监控（已在 README 诚实标注）

---

## 三、实习项目（1-2 分钟，简短但要讲实）

> 优联软件 4 个月实习，我用 Java + LangChain4j + 阿里云百炼做了个销售专员 Agent：
> - 用户自然语言意图 → Function Calling 调用商户查询/到店预约工具
> - Redis 按 sessionId 持久化对话历史，支持跨轮代指消解（用户说"去店里"能关联上轮提的商户）
> - Caffeine 本地一级缓存 + Redis 分布式二级缓存降延迟
> - 消息队列做异步削峰，处理高并发咨询写入
>
> 我从中验证了：Function Calling 在中文意图下的可靠性边界、对话历史持久化的 key 设计、以及缓存命中率对首屏延迟的影响。

要点：把"我具体做了什么 + 解决了什么"讲清楚，不要只列技术名词。

---

## 四、高频追问与标准答案（按概率排序）

| 追问 | 标准答案骨架 |
|------|------------|
| 为什么切分用段落优先 + 句切 + 重叠？ | 段落被拦腰切断会破坏语义完整性；句切保证长段也被切到完整句；重叠只用于"长段被切断"这一处，跨段不补 |
| 三级 JSON 容错怎么设计的？ | 直接解析 → 去围栏 → 括号配对扫描。配对扫描要跳过字符串内的花括号（Python 实现见 `core/llm.py:_extract_first_object`） |
| 为什么不用 LangChain / LlamaIndex？ | 项目用直接 OpenAI SDK，更薄、更可控、出问题易定位；实习用 LangChain4j 是 Java 生态权衡（取舍说明） |
| 多厂商怎么做的？ | OpenAI 兼容协议 + Settings 集中配置 + `PROVIDER_PRESETS` 字典；切换厂商改 .env 一行 |
| 检索评测怎么做？ | `scripts/eval_retrieval.py`：离线覆盖检查（不联网）+ 在线 Recall@k |
| 怎么排查召回质量问题？ | 看命中的 source/domain 对照预期知识；调 chunk_size/overlap；rerank α 调参；加测试用例固化 |
| 模型路由为什么不上小模型？ | 规则路由零成本、可复现、面试可讲清楚；小模型多一次调用、引入额外故障域；取舍原则：先简单方案再升级 |
| 成本怎么算？ | 用量统计在 `/api/usage` + 前端看板；按任务复杂度路由轻/重模型能省 50-90%（看场景） |
| 项目能演示吗？ | 能。`uvicorn app.main:app`，浏览器打开 `http://127.0.0.1:8000`，6 个 Agent 都可点"填入示例 → 运行分析" |
| 单点故障在哪？ | ChromaDB 本地持久化、向量库无外部依赖；生产应补 Redis 缓存层、K8s 多副本（已在 roadmap 标注） |

---

## 五、诚实边界（不要过度吹）

- 学校：广州软件学院本科，问到就说，**不要回避也不要多讲**——简历已经写了
- 实习：4 个月优联，不是大厂，但用了 LangChain4j + Function Calling 真实场景，**比单纯"参与过 XX"有说服力**
- 项目：是个人项目 + 演示级，**不是生产级**。主动说"生产部署需补 JWT/限流/监控"，体现工程成熟度
- 代码量：后端 ~2.8k 行 / 前端 ~1.7k 行，**不含 1107 行的文档生成脚本**——这是合理规模
- 经验值数字（如"成本下降 50-90%"）要诚实加"看任务分布"

---

## 六、反问环节（选 2-3 个问，体现你有判断）

不要问薪资福利、不要问"团队氛围好不好"。要问有信息密度的：

1. **业务侧**：「我看咱们在招后端，这个岗位进来主要会做哪类系统？订单/ERP，还是偏向 AI 平台？」
2. **技术侧**：「团队里 AI 应用这块目前由谁负责、用什么栈、有没有内部知识库或 RAG 平台可以基于？」（判断公司 AI 成熟度）
3. **成长侧**：「团队对入职 1-2 年的工程师有没有具体的培养计划？code review 机制是怎样的？」（判断有没有 mentor）

---

## 七、面试前 30 分钟必做

1. **跑一遍项目**：`uvicorn app.main:app`，浏览器打开 `http://127.0.0.1:8000`，每个 Agent 都点一下确认能跑
2. **预演 5 分钟讲解**：对着镜子或录音讲一遍核心项目，卡壳的地方回去读源码
3. **看一遍代码评审报告**：`AGENT_APP_代码质量评估报告.md`——证明你做过代码 review，能讲出"我修了什么、为什么"
4. **准备 3 个反问**：基于上面六、选 2-3 个
5. **打印 / 熟悉简历**：尤其是项目描述里的每个数字（21 篇 / 299 切片 / 6/6 100% / 77 测试），被追问时要能脱口解释

## 八、一句话收尾（面试官问"你还有什么想问的"之后）

> 没有其他问题了，感谢您的时间。我对这个岗位方向非常感兴趣，希望能有机会加入团队一起把 AI 工程化做扎实。

---

**最后提醒**：面试不是背稿，是对话。把上面的骨架变成你自己的话，不要一字一句念。被追问时诚实比圆滑更重要——"这一块我当时没考虑到"比硬编一个答案得分高得多。